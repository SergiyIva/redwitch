import "@testing-library/jest-dom/extend-expect";

it("Consumer with all sub-component tree correct rendering", () => {
  // const client = createMockClient({}, typeDefs);
  // render(
  //   <ThemeProvider theme={lightTheme}>
  //     <ApolloProvider client={client}>
  //       <Consumer />
  //     </ApolloProvider>
  //   </ThemeProvider>
  // );
});
