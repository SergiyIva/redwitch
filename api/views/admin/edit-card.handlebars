<div class="pt-3 pb-2 mb-3 border-bottom">
        <h2 class="h2">Редактировать услугу</h2>
</div>
        <div class="py-3 text-center">
            <p class="lead">Измените значения в полях формы, что представлена ниже. Нажмите кнопку "Сохранить".</p>
        </div>
        <div class="row g-5 pb-5">
            <div class="col-md-7 col-lg-8">
                <h4 class="mb-3">Новые данные</h4>
                <form class="needs-validation" method="post" action="/admin/edit-card" role="form"
                    enctype="multipart/form-data" >
                    <div class="row g-3">
                        <input type="hidden" name="_csrf" value="{{csrf}}">
                        <input type="hidden" name="_id" value="{{_id}}">
                        <input type="hidden" name="history" value="/admin/main">
                        <div class="col-12">
                            <label for="title" class="form-label">Название <span class="text-muted">(на русском)</span></label>
                            <input name="title" value="{{title}}" type="text" class="form-control" id="title" placeholder="Название" required>
                            <div class="invalid-feedback">
                                Please enter a valid email address for shipping updates.
                            </div>
                        </div>
                        <div class="col-12">
                            <label for="slug" class="form-label">Краткое описание</label>
                            <input name="slug"  type="text" class="form-control" id="slug" placeholder="Краткое описание" required value='{{slug}}'>
                            <div class="invalid-feedback">
                                Please enter a valid email address for shipping updates.
                            </div>
                        </div>
                        <div class="input-group">
                            <span class="input-group-text">Описание</span>
                            <textarea name="describe" class="form-control" aria-label="With textarea" style="height: 80px;" required>{{describe}}</textarea>
                        </div>
                        <div class="col-12">
                            <label for="price" class="form-label">Стоимость</label>
                            <input name="price" value="{{price}}" type="text" class="form-control" id="price" placeholder="Стоимость" required>
                            <div class="invalid-feedback">
                                Please enter a valid email address for shipping updates.
                            </div>
                        </div>
                        <div class="col-12">
                            <label for="tags" class="form-label">Тэги <span class="text-muted">(через запятую)</span></label>
                            <input name="tags" value="{{tags}}" type="text" class="form-control" id="tags" placeholder="Тэги" required>
                            <div class="invalid-feedback">
                                Please enter a valid email address for shipping updates.
                            </div>
                        </div>
                        <div class="col-12">
                            <input name="available" {{#if available}}checked{{/if}} type="checkbox" class="form-check-input" id="same-address">
                            <label class="form-check-label" for="same-address">Сделать доступным для покупки.</label>
                        </div>
                        <div class="mb-3">
                            <label class="form-label" for="customFile" >Изображение</label>
                            <input name="image" type="file" class="form-control" id="customFile" accept="image/*">
                        </div>
                    </div>
                    <hr class="my-4">
                    <button class="w-100 btn btn-primary " type="submit">Сохранить</button>
                    <button type="button" class="btn btn-outline-danger w-100 mt-3" data-bs-toggle="modal" data-bs-target="#exampleModalDefault">
                        Удалить
                    </button>
                </form>
            </div>
        </div>
<div class="modal fade" id="exampleModalDefault" tabindex="-1" aria-labelledby="exampleModalLabel" style="display: none;" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="exampleModalLabel">Подтвердить удаление</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Вы действительно хотите удалить услугу {{title}}? Данные невозможно будет восстановить.
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Отмена</button>
                <button id="delBtn" type="button" class="btn btn-primary">Подтвердить</button>
            </div>
        </div>
    </div>
</div>

<script>
    const delbtn = document.getElementById("delBtn")
    delbtn.addEventListener("click", (e)=>{
        e.preventDefault()
        console.log("Delete")
    })
</script>